# Lesson 3: Client-Server Architecture 🌐

<div dir="rtl">

## المقدمة

**Client-Server Architecture** هي الأساس الذي تبنى عليه معظم التطبيقات الحديثة.

في هذا الدرس، سنفهم بعمق كيف يتواصل Client مع Server، وما هي المكونات الأساسية لهذه المعمارية.

</div>

---

## 🏗️ What is Client-Server Architecture?

<div dir="rtl">

### التعريف البسيط:

نظام يتكون من:

- **Client** (العميل): من يطلب الخدمة
- **Server** (الخادم): من يقدم الخدمة

</div>

```
┌─────────────┐                    ┌─────────────┐
│             │   ──► Request ──►   │             │
│   CLIENT    │                     │   SERVER    │
│             │   ◄── Response ◄──  │             │
└─────────────┘                    └─────────────┘
```

<div dir="rtl">

### مثال من الحياة اليومية:

**المطعم:**

- **أنت (Client)**: "أريد بيتزا بالجبنة"
- **الطباخ (Server)**: "تفضل بيتزتك"

**البنك:**

- **أنت (Client)**: "أريد سحب 1000 جنيه"
- **الموظف/ATM (Server)**: "تفضل"

**التطبيقات:**

- **Browser (Client)**: "أريد صفحة Google الرئيسية"
- **Google Server**: "تفضل الصفحة"

</div>

---

## 🔍 Components Deep Dive

### 1. The Client 📱

<div dir="rtl">

**التعريف:** أي برنامج/جهاز يطلب خدمة من Server.

**أمثلة:**

</div>

| Type            | Example                 |
| --------------- | ----------------------- |
| **Web Browser** | Chrome, Firefox, Safari |
| **Mobile App**  | Facebook App, Instagram |
| **Desktop App** | Slack, Discord, VS Code |
| **API Client**  | Postman, curl           |
| **IoT Device**  | Smart TV, Smart Watch   |

<div dir="rtl">

**مسؤوليات Client:**

- ✅ **UI/UX**: عرض المعلومات بشكل جميل
- ✅ **User Input**: جمع بيانات من المستخدم
- ✅ **Send Requests**: إرسال طلبات للـ Server
- ✅ **Handle Responses**: معالجة الردود
- ✅ **Local Storage**: حفظ بيانات مؤقتة (Cache)

**خصائص Client:**

- 🔴 **Stateless**: لا يحفظ حالة Session
- 💪 **Thin**: منطق بسيط، العبء على Server
- 🔌 **Dependent**: يعتمد على Server للبيانات

</div>

---

### 2. The Server 🖥️

<div dir="rtl">

**التعريف:** برنامج يستقبل Requests ويرد بـ Responses.

**أنواع Servers:**

</div>

| Server Type            | Purpose                                      | Example           |
| ---------------------- | -------------------------------------------- | ----------------- |
| **Web Server**         | <div dir="rtl">تقديم ملفات HTML/CSS/JS</div> | Nginx, Apache     |
| **Application Server** | <div dir="rtl">تنفيذ Backend Logic</div>     | Our Go Server     |
| **Database Server**    | <div dir="rtl">تخزين واسترجاع البيانات</div> | PostgreSQL, MySQL |
| **File Server**        | <div dir="rtl">تخزين الملفات</div>           | AWS S3, FTP       |
| **Email Server**       | <div dir="rtl">إرسال/استقبال Emails</div>    | SMTP, Gmail       |

<div dir="rtl">

**مسؤوليات Server:**

- ✅ **Request Handling**: استقبال ومعالجة الطلبات
- ✅ **Business Logic**: تنفيذ المنطق البرمجي
- ✅ **Database Operations**: التعامل مع قواعد البيانات
- ✅ **Authentication**: التحقق من الهوية
- ✅ **Data Processing**: معالجة وتحليل البيانات
- ✅ **Response Building**: بناء وإرسال الردود

**خصائص Server:**

- 💪 **Powerful**: معالج قوي، ذاكرة كبيرة
- 🔄 **Always On**: يعمل 24/7
- 🏢 **Centralized**: نقطة مركزية للبيانات
- 🔐 **Secure**: يحتوي على البيانات الحساسة

</div>

---

## 🔄 The Request-Response Cycle

<div dir="rtl">

### الدورة الكاملة بالتفصيل:

</div>

```
┌────────────────────────────────────────────────────────────┐
│ STEP 1: User Action                                        │
│ User clicks "View Products" button                         │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 2: Client Prepares Request                            │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ Browser (Client):                                      │ │
│ │ - Method: GET                                          │ │
│ │ - URL: https://shop.com/api/products                   │ │
│ │ - Headers: { Authorization: "Bearer xxx", ... }        │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 3: DNS Lookup (Domain → IP)                           │
│ shop.com → 192.168.1.100                                   │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 4: TCP Connection (3-Way Handshake)                   │
│ Client ──► SYN ──────────────► Server                      │
│ Client ◄── SYN-ACK ───────────◄ Server                     │
│ Client ──► ACK ──────────────► Server                      │
│ ✅ Connection Established                                  │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 5: HTTPS Handshake (if HTTPS)                         │
│ - SSL/TLS Negotiation                                      │
│ - Certificate Exchange                                     │
│ - Encryption Established                                   │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 6: Send HTTP Request                                  │
│ GET /api/products HTTP/1.1                                 │
│ Host: shop.com                                             │
│ Authorization: Bearer xxx                                  │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 7: Server Receives Request                            │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ Server (Go Application):                               │ │
│ │ 1. Parse Request                                       │ │
│ │ 2. Verify JWT Token (Authentication)                  │ │
│ │ 3. Check Permissions (Authorization)                  │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 8: Business Logic Execution                           │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ func GetProducts(c *gin.Context) {                     │ │
│ │     // Apply filters                                   │ │
│ │     category := c.Query("category")                    │ │
│ │     minPrice := c.Query("min_price")                   │ │
│ │                                                         │ │
│ │     // Query database...                               │ │
│ │ }                                                       │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 9: Database Query                                     │
│ Server  ──► SELECT * FROM products WHERE ...  ──► Database │
│ Server  ◄── [Product 1, Product 2, ...]      ◄── Database │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 10: Build Response                                    │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ HTTP/1.1 200 OK                                        │ │
│ │ Content-Type: application/json                         │ │
│ │                                                         │ │
│ │ {                                                       │ │
│ │   "success": true,                                     │ │
│ │   "data": [...products...],                            │ │
│ │   "total": 150                                         │ │
│ │ }                                                       │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 11: Send Response to Client                           │
│ Server ──────► HTTP Response ──────► Client                │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 12: Client Receives & Processes                       │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ const data = await response.json();                    │ │
│ │ displayProducts(data.data);                            │ │
│ │ updateUI();                                            │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────────────┐
│ STEP 13: User Sees Results                                 │
│ 📦 Product 1 - 1000 EGP                                    │
│ 📦 Product 2 - 1500 EGP                                    │
│ 📦 Product 3 - 2000 EGP                                    │
└────────────────────────────────────────────────────────────┘
```

---

## 🌍 Client-Server Models

### 1. Two-Tier Architecture

```
┌──────────────┐         ┌──────────────┐
│              │         │              │
│    Client    │ ◄────► │    Server    │
│  (Browser)   │         │  + Database  │
│              │         │              │
└──────────────┘         └──────────────┘
```

<div dir="rtl">

**الاستخدام:** تطبيقات بسيطة

**المميزات:**

- ✅ بسيطة
- ✅ سهلة الإعداد

**العيوب:**

- ❌ صعب التوسع (Scaling)
- ❌ كل الحمل على سيرفر واحد

</div>

---

### 2. Three-Tier Architecture ⭐ (الأكثر شيوعاً)

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│              │     │              │     │              │
│ Presentation │◄───►│  Application │◄───►│    Data      │
│    Layer     │     │    Layer     │     │    Layer     │
│  (Frontend)  │     │  (Backend)   │     │  (Database)  │
│              │     │              │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
```

<div dir="rtl">

**Layers:**

**1. Presentation Layer (Frontend):**

- UI/UX
- User interaction
- Data display

**2. Application Layer (Backend):**

- Business logic
- API endpoints
- Authentication

**3. Data Layer (Database):**

- Data storage
- Data retrieval
- Data integrity

**المميزات:**

- ✅ سهل الصيانة (كل layer منفصل)
- ✅ يمكن توسيع كل layer على حدة
- ✅ أمان أفضل

</div>

---

### 3. N-Tier Architecture (Enterprise)

```
┌───────────┐   ┌───────────┐   ┌───────────┐   ┌──────────┐
│           │   │           │   │           │   │          │
│  Client   │◄─►│   Load    │◄─►│ Backend   │◄─►│ Database │
│           │   │  Balancer │   │  Servers  │   │ Cluster  │
│           │   │           │   │  (Pool)   │   │          │
└───────────┘   └───────────┘   └───────────┘   └──────────┘
                                      │
                                      ▼
                              ┌───────────────┐
                              │ Cache (Redis) │
                              └───────────────┘
                                      │
                                      ▼
                              ┌───────────────┐
                              │ Message Queue │
                              └───────────────┘
```

<div dir="rtl">

**للتطبيقات الكبيرة:** Facebook, Google, Amazon

</div>

---

## 💡 Key Concepts

### 1. Stateless Communication

<div dir="rtl">

**معناها:** Server لا يحفظ أي معلومات عن Client بين Requests.

</div>

```
Request 1: User logs in
Server: ✅ "Login successful, here's a token"
Server forgets everything!

Request 2: User gets profile (with token)
Server: 🤔 "Who are you? Show me the token"
Server checks token, returns profile
Server forgets everything again!
```

<div dir="rtl">

**الحل:** Tokens (JWT) أو Cookies

</div>

---

### 2. Synchronous vs Asynchronous

**Synchronous (Blocking):**

```javascript
// Frontend waits
const response = await fetch("/api/data"); // ⏸️ Waiting...
console.log("Done!"); // Only runs after response
```

**Asynchronous (Non-Blocking):**

```javascript
// Frontend continues
fetch("/api/data").then((response) => {
  console.log("Got response!");
});
console.log("Doing other things..."); // Runs immediately
```

---

### 3. HTTP Protocol

<div dir="rtl">

**هو "اللغة" التي يتكلم بها Client مع Server.**

</div>

```
Client: "GET /api/users HTTP/1.1"
Server: "HTTP/1.1 200 OK ... [data]"
```

<div dir="rtl">

**(سنتعلمه بالتفصيل في Module 1.2)**

</div>

---

## 🔐 Security في Client-Server

### 1. HTTPS (HTTP Secure)

```
Without HTTPS ❌:
Client ──► email: ahmed@test.com    ──► Server
         ──► password: mypass123    ──►

👁️ أي حد في المنتصف يشوف البيانات!

With HTTPS ✅:
Client ──► 🔒 encrypted data 🔒 ──► Server

🛡️ مشفرة، لا يمكن قراءتها
```

---

### 2. Authentication Tokens

<div dir="rtl">

بدلاً من إرسال Password مع كل Request:

</div>

```
Login Request:
Client ──► email + password ──► Server
Client ◄── JWT Token ◄────────── Server

Subsequent Requests:
Client ──► Token in Header ──► Server
         ✅ Server verifies token
```

---

## 💡 Key Takeaways

<div dir="rtl">

- ✅ **Client** يطلب، **Server** يرد
- ✅ **Request-Response Cycle** هو الأساس
- ✅ **Three-Tier** الأكثر شيوعاً (Frontend, Backend, Database)
- ✅ **Stateless**: Server لا يحفظ حالة بين Requests
- ✅ **HTTP** هو البروتوكول المستخدم
- ✅ **HTTPS** للأمان (encryption)
- ✅ Client و Server يمكن أن يكونوا في أماكن جغرافية مختلفة

</div>

---

## ⏭️ Next Lesson

<div dir="rtl">

الآن بعد أن فهمت المعمارية، دعنا نتعمق في مسؤوليات Backend:

**➡️ [Lesson 4: Backend Responsibilities](./04-backend-responsibilities.md)**

</div>

---

<div align="center">

[⬅️ Previous: Frontend vs Backend](./02-frontend-vs-backend.md) | [📚 Module Home](../README.md)

</div>
