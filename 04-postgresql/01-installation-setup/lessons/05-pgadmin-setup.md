# إعداد pgAdmin 🖥️

<div dir="rtl">

## مقدمة

**pgAdmin** هي الأداة الرسمية لإدارة PostgreSQL بواجهة رسومية. بتخليك تعمل كل حاجة بالـ Mouse بدل ما تكتب أوامر.

**المدة المتوقعة:** 10-15 دقيقة

</div>

---

## 🤔 ليه pgAdmin؟

<div dir="rtl">

### مقارنة: pgAdmin vs psql

```
┌─────────────────────────────────────────────────────────────┐
│                     psql (Command Line)                      │
├─────────────────────────────────────────────────────────────┤
│  postgres=# SELECT * FROM users;                            │
│                                                              │
│   id |   name   |      email       | age                    │
│  ----+----------+------------------+-----                    │
│    1 | Ahmed    | ahmed@test.com   |  25                    │
│    2 | Sara     | sara@test.com    |  28                    │
│                                                              │
│  ✅ سريع للخبراء                                            │
│  ❌ صعب للمبتدئين                                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    pgAdmin (GUI)                             │
├─────────────────────────────────────────────────────────────┤
│  ┌────────────┐  ┌─────────────────────────────────┐        │
│  │ Servers    │  │  Query Tool                     │        │
│  │ ├─ Local   │  │  ┌─────────────────────────────┐│        │
│  │ │  └─ mydb │  │  │ SELECT * FROM users;       ││        │
│  │ └─ Prod    │  │  └─────────────────────────────┘│        │
│  └────────────┘  │  ┌─────────────────────────────┐│        │
│                   │  │ id │ name  │ email        ││        │
│                   │  │  1 │ Ahmed │ ahmed@...    ││        │
│                   │  └─────────────────────────────┘│        │
│                   └─────────────────────────────────┘        │
│  ✅ سهل ومرئي                                               │
│  ✅ تصدير النتائج                                           │
│  ✅ تصميم الجداول بصرياً                                    │
└─────────────────────────────────────────────────────────────┘
```

</div>

---

## 📥 تثبيت pgAdmin

<div dir="rtl">

### على Windows

لو ثبّت PostgreSQL من الـ Installer الرسمي، pgAdmin مثبت بالفعل!

**لو مش موجود:**
1. روح على: https://www.pgadmin.org/download/
2. حمّل pgAdmin 4 for Windows
3. ثبّته عادي

### على macOS

</div>

```bash
# Homebrew
brew install --cask pgadmin4

# أو حمّل من الموقع
# https://www.pgadmin.org/download/pgadmin-4-macos/
```

<div dir="rtl">

### على Linux

</div>

```bash
# Ubuntu/Debian
curl -fsS https://www.pgadmin.org/static/packages_pgadmin_org.pub | \
    sudo gpg --dearmor -o /usr/share/keyrings/packages-pgadmin-org.gpg

sudo sh -c 'echo "deb [signed-by=/usr/share/keyrings/packages-pgadmin-org.gpg] https://ftp.postgresql.org/pub/pgadmin/pgadmin4/apt/$(lsb_release -cs) pgadmin4 main" > /etc/apt/sources.list.d/pgadmin4.list'

sudo apt update
sudo apt install pgadmin4-desktop  # Desktop version
# أو
sudo apt install pgadmin4-web      # Web version
```

---

## 🚀 أول تشغيل

<div dir="rtl">

### 1. افتح pgAdmin

- **Windows:** من Start Menu → pgAdmin 4
- **macOS:** من Applications → pgAdmin 4
- **Linux:** من Applications menu أو `pgadmin4`

### 2. إنشاء Master Password

أول مرة تفتح pgAdmin، هيطلب منك **Master Password**:

```
┌─────────────────────────────────────────────┐
│  Set Master Password                         │
├─────────────────────────────────────────────┤
│                                              │
│  Password: ••••••••••                        │
│  Confirm:  ••••••••••                        │
│                                              │
│  ⚠️ هذا الـ Password لـ pgAdmin نفسه        │
│  مش لـ PostgreSQL!                          │
│                                              │
│  [OK]                                        │
└─────────────────────────────────────────────┘
```

> 💡 **ملاحظة:** الـ Master Password ده لحماية الـ saved passwords في pgAdmin. اختار حاجة مختلفة عن password الـ postgres user.

</div>

---

## 🔗 الاتصال بـ PostgreSQL Server

<div dir="rtl">

### الخطوة 1: إضافة Server جديد

</div>

```
┌─────────────────────────────────────────────────────────────┐
│  Browser (الشريط الجانبي)                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Servers                                                     │
│  └─ (right-click) → Register → Server...                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

<div dir="rtl">

### الخطوة 2: إعدادات الـ Server

**Tab: General**

</div>

```
┌─────────────────────────────────────────────┐
│  General                                     │
├─────────────────────────────────────────────┤
│  Name: Local PostgreSQL                      │
│  └── (اختار اسم تعرفه بيه)                   │
│                                              │
│  Server group: Servers                       │
│  └── (خليها default)                         │
│                                              │
│  Background: [color picker]                  │
│  └── (لون مميز للـ server)                  │
└─────────────────────────────────────────────┘
```

<div dir="rtl">

**Tab: Connection**

</div>

```
┌─────────────────────────────────────────────┐
│  Connection                                  │
├─────────────────────────────────────────────┤
│  Host name/address: localhost               │
│  Port: 5432                                  │
│  Maintenance database: postgres              │
│  Username: postgres                          │
│  Password: ••••••••••                        │
│                                              │
│  ☑️ Save password                            │
│                                              │
└─────────────────────────────────────────────┘
```

<div dir="rtl">

**اضغط Save**

### الخطوة 3: التحقق من الاتصال

لو كل حاجة صح، هتشوف:

</div>

```
Servers
└─ Local PostgreSQL
   └─ Databases
      ├─ postgres
      └─ (your databases)
   └─ Login/Group Roles
   └─ Tablespaces
```

---

## 🧭 التنقل في pgAdmin

<div dir="rtl">

### الواجهة الرئيسية

</div>

```
┌─────────────────────────────────────────────────────────────────────┐
│  pgAdmin 4                                                    [─][□][×] │
├─────────────────────────────────────────────────────────────────────┤
│  File   Object   Tools   Help                                        │
├────────────────┬────────────────────────────────────────────────────┤
│  Browser       │   Dashboard / Properties / SQL / Statistics        │
│                │                                                     │
│  ▼ Servers     │   ┌───────────────────────────────────────────┐   │
│    ▼ Local     │   │                                           │   │
│      ▼ Databases│   │           Server Dashboard                │   │
│        ▼ myapp │   │                                           │   │
│          Schemas│   │   Sessions: 5    Transactions: 128       │   │
│          ▼public│   │   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │   │
│            Tables│   │                                           │   │
│            Views│   └───────────────────────────────────────────┘   │
│            ...  │                                                     │
│                │                                                     │
└────────────────┴────────────────────────────────────────────────────┘
```

<div dir="rtl">

### العناصر الأساسية

| العنصر | الوظيفة |
|--------|---------|
| **Browser** | شجرة التنقل - كل الـ Servers والـ Databases |
| **Dashboard** | معلومات عن الـ Server (connections, activity) |
| **Properties** | خصائص العنصر المختار |
| **SQL** | الـ SQL المستخدم لإنشاء العنصر |
| **Query Tool** | لكتابة وتنفيذ SQL |

</div>

---

## ✏️ استخدام Query Tool

<div dir="rtl">

### فتح Query Tool

</div>

```
1. اختار Database (مثلاً: postgres)
2. Right-click → Query Tool
   أو: Tools menu → Query Tool
   أو: Ctrl+Shift+Q (Windows) / Cmd+Shift+Q (Mac)
```

<div dir="rtl">

### واجهة Query Tool

</div>

```
┌─────────────────────────────────────────────────────────────────────┐
│  Query Tool - postgres/postgres@Local                               │
├─────────────────────────────────────────────────────────────────────┤
│  [▶ Execute] [⬛ Stop] [💾 Save] [📂 Open] [Explain]                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  SELECT * FROM users WHERE age > 20;                                │
│  █                                                                   │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  Data Output           Messages          Notifications              │
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────┬──────────┬─────────────────┬───────┐                      │
│  │  id  │   name   │     email       │  age  │                      │
│  ├──────┼──────────┼─────────────────┼───────┤                      │
│  │   1  │  Ahmed   │  ahmed@test.com │   25  │                      │
│  │   2  │  Sara    │  sara@test.com  │   28  │                      │
│  └──────┴──────────┴─────────────────┴───────┘                      │
│                                                                      │
│  Successfully run. Total query runtime: 12 msec. 2 rows affected.   │
└─────────────────────────────────────────────────────────────────────┘
```

<div dir="rtl">

### اختصارات مهمة

| الاختصار | الوظيفة |
|----------|---------|
| `F5` | Execute Query |
| `Ctrl+S` | Save Query |
| `Ctrl+Shift+E` | Explain Query |
| `Ctrl+/` | Comment/Uncomment |
| `Ctrl+Space` | Autocomplete |

</div>

---

## 🗃️ إنشاء Database من pgAdmin

<div dir="rtl">

### الطريقة 1: بالـ GUI

</div>

```
1. Databases → Right-click → Create → Database...

┌─────────────────────────────────────────────┐
│  Create - Database                           │
├─────────────────────────────────────────────┤
│  General                                     │
│  ├─ Database: myapp                          │
│  ├─ Owner: postgres                          │
│  └─ Comment: My application database         │
│                                              │
│  Definition                                  │
│  ├─ Encoding: UTF8                           │
│  ├─ Template: template0                      │
│  └─ Collation: en_US.UTF-8                  │
│                                              │
│  [Save]  [Cancel]                            │
└─────────────────────────────────────────────┘
```

<div dir="rtl">

### الطريقة 2: بالـ SQL

</div>

```sql
-- في Query Tool
CREATE DATABASE myapp
    WITH
    OWNER = postgres
    ENCODING = 'UTF8'
    CONNECTION LIMIT = -1;
```

---

## 📋 إنشاء Table من pgAdmin

<div dir="rtl">

### الطريقة 1: بالـ GUI

</div>

```
1. myapp → Schemas → public → Tables → Right-click → Create → Table...

┌─────────────────────────────────────────────────────────────────┐
│  Create - Table                                                  │
├─────────────────────────────────────────────────────────────────┤
│  General                                                         │
│  └─ Name: users                                                  │
│                                                                  │
│  Columns (Tab)                                                   │
│  ┌────────────┬──────────────┬──────────┬─────────────┐         │
│  │    Name    │  Data type   │ Not Null │ Primary key │         │
│  ├────────────┼──────────────┼──────────┼─────────────┤         │
│  │ id         │ serial       │    ✓     │      ✓      │         │
│  │ name       │ varchar(100) │    ✓     │             │         │
│  │ email      │ varchar(100) │    ✓     │             │         │
│  │ created_at │ timestamp    │          │             │         │
│  └────────────┴──────────────┴──────────┴─────────────┘         │
│                                                                  │
│  [+ Add]  [Save]  [Cancel]                                       │
└─────────────────────────────────────────────────────────────────┘
```

<div dir="rtl">

### الطريقة 2: بالـ SQL (أسرع)

</div>

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 📊 عرض وتعديل البيانات

<div dir="rtl">

### عرض البيانات

</div>

```
1. Tables → users → Right-click → View/Edit Data → All Rows

┌─────────────────────────────────────────────────────────────────┐
│  Edit Data - users                                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────┬──────────┬─────────────────┬─────────────────────┐    │
│  │  id  │   name   │     email       │     created_at      │    │
│  ├──────┼──────────┼─────────────────┼─────────────────────┤    │
│  │   1  │  Ahmed   │  ahmed@test.com │  2024-12-21 10:30  │    │
│  │   2  │  Sara    │  sara@test.com  │  2024-12-21 11:45  │    │
│  │   *  │          │                 │                     │    │ ← سطر جديد
│  └──────┴──────────┴─────────────────┴─────────────────────┘    │
│                                                                  │
│  [💾 Save] [🔄 Refresh] [🗑️ Delete]                              │
└─────────────────────────────────────────────────────────────────┘
```

<div dir="rtl">

### تعديل البيانات

1. **Double-click** على الخلية لتعديلها
2. اكتب القيمة الجديدة
3. اضغط **Enter**
4. اضغط **Save (F6)** لحفظ التغييرات

</div>

---

## 💾 تصدير واستيراد البيانات

<div dir="rtl">

### تصدير البيانات (Export)

</div>

```
1. من Query Tool بعد تنفيذ Query
2. اضغط على زر Download (↓)
3. اختار الصيغة:
   - CSV
   - JSON
   - Text

أو:

1. Table → Right-click → Import/Export Data...
2. اختار Export
3. حدد الإعدادات والمسار
```

<div dir="rtl">

### استيراد البيانات (Import)

</div>

```
1. Table → Right-click → Import/Export Data...
2. اختار Import
3. حدد الملف (CSV, TXT, etc.)
4. طابق الأعمدة
5. Import
```

---

## 🔧 إعدادات مفيدة

<div dir="rtl">

### تغيير Theme

</div>

```
File → Preferences → Miscellaneous → Themes
- Standard (Light)
- Dark
- High Contrast
```

<div dir="rtl">

### تغيير الخط

</div>

```
File → Preferences → Query Tool → Editor → Font
```

<div dir="rtl">

### Auto-complete

</div>

```
File → Preferences → Query Tool → Auto-completion
- ✅ Keywords in uppercase
- ✅ Autocomplete on key press
```

---

## ❌ حل المشاكل الشائعة

<div dir="rtl">

### مشكلة 1: Unable to connect to server

</div>

```
┌─────────────────────────────────────────────────────────────┐
│  Error:                                                      │
│  could not connect to server: Connection refused             │
│  Is the server running on host "localhost" and accepting    │
│  TCP/IP connections on port 5432?                           │
└─────────────────────────────────────────────────────────────┘

الحل:
1. تأكد إن PostgreSQL شغال
   - Windows: services.msc → postgresql
   - Linux: sudo systemctl status postgresql

2. تأكد من الـ Port
3. تأكد من pg_hba.conf
```

<div dir="rtl">

### مشكلة 2: Password authentication failed

</div>

```
الحل:
1. تأكد من الـ Password الصحيحة
2. جرب reset الـ Password من psql:
   ALTER USER postgres WITH PASSWORD 'new_password';
```

<div dir="rtl">

### مشكلة 3: pgAdmin بطيء

</div>

```
الحل:
1. File → Preferences → Browser → Show system objects: OFF
2. قلل عدد rows المعروضة
3. أقفل Query Tools مش محتاجها
```

---

## 🛠️ بدائل لـ pgAdmin

<div dir="rtl">

| الأداة | المميزات | السعر |
|--------|----------|-------|
| **pgAdmin** | الرسمية، شاملة | مجاني |
| **DBeaver** | يدعم databases كتير | مجاني |
| **DataGrip** | من JetBrains، قوي جداً | مدفوع |
| **TablePlus** | بسيط وأنيق | مدفوع (free tier) |
| **Postico** | لـ macOS فقط | مدفوع |

</div>

---

## ✅ Checklist

<div dir="rtl">

- [ ] ✅ pgAdmin installed
- [ ] ✅ Master Password configured
- [ ] ✅ Connected to PostgreSQL server
- [ ] ✅ يقدر يشوف الـ Databases
- [ ] ✅ يقدر يفتح Query Tool
- [ ] ✅ يقدر ينفذ SQL

</div>

---

## ⏭️ الدرس التالي

<div dir="rtl">

**➡️ [أوامر psql الأساسية](./06-psql-commands.md)**

</div>

---

<div align="center">

[⬅️ السابق: تثبيت على macOS](./04-installation-macos.md) | [🏠 العودة للـ Module](../README.md)

</div>
