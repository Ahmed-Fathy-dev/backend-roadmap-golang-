# تثبيت PostgreSQL على Windows 🪟

<div dir="rtl">

## مقدمة

في الدرس ده هنتعلم خطوة بخطوة إزاي نثبت PostgreSQL على Windows. هنغطي كل التفاصيل من التحميل لحد التأكد إن كل حاجة شغالة تمام.

**المدة المتوقعة:** 15-20 دقيقة

</div>

---

## 📋 المتطلبات

<div dir="rtl">

### متطلبات النظام

```
┌─────────────────────────────────────────────┐
│  Windows 10/11 (64-bit)                     │
│  RAM: 2GB minimum (4GB+ recommended)        │
│  Disk: 512MB free space                     │
│  Admin privileges                           │
└─────────────────────────────────────────────┘
```

### قبل ما تبدأ

- ✅ تأكد إنك Administrator على الجهاز
- ✅ أقفل أي Antivirus مؤقتاً (بيعمل مشاكل أحياناً)
- ✅ جهّز password قوية للـ postgres user

</div>

---

## 📥 الخطوة 1: تحميل PostgreSQL

<div dir="rtl">

### من الموقع الرسمي

1. **افتح المتصفح وروح على:**
   ```
   https://www.postgresql.org/download/windows/
   ```

2. **اضغط على "Download the installer"**

3. **اختار الإصدار:**
   ```
   ┌─────────────────────────────────────────────┐
   │  PostgreSQL Version    │  Windows x86-64   │
   ├─────────────────────────────────────────────┤
   │  16.x (Latest)         │  ✅ اختار ده      │
   │  15.x                   │  ممكن            │
   │  14.x                   │  قديم شوية       │
   └─────────────────────────────────────────────┘
   ```

4. **الملف هيكون اسمه حاجة زي:**
   ```
   postgresql-16.1-1-windows-x64.exe
   (حجمه حوالي 300MB)
   ```

</div>

---

## ⚙️ الخطوة 2: تشغيل الـ Installer

<div dir="rtl">

### بدء التثبيت

1. **Double-click على الملف اللي حمّلته**

2. **هتظهر نافذة UAC (User Account Control)**
   - اضغط **Yes**

3. **شاشة Welcome**
   - اضغط **Next**

</div>

---

## 📁 الخطوة 3: اختيار مسار التثبيت

<div dir="rtl">

### Installation Directory

```
Default Path:
C:\Program Files\PostgreSQL\16

┌─────────────────────────────────────────────────────┐
│  💡 نصيحة: خلّي المسار Default                      │
│                                                      │
│  ❌ تجنب:                                           │
│  - مسارات فيها مسافات كتير                          │
│  - مسارات فيها حروف عربية                           │
│  - OneDrive أو مجلدات synchronized                  │
└─────────────────────────────────────────────────────┘
```

اضغط **Next**

</div>

---

## 🧩 الخطوة 4: اختيار المكونات

<div dir="rtl">

### Select Components

```
┌─────────────────────────────────────────────────────┐
│  ☑️  PostgreSQL Server    ─── السيرفر الأساسي      │
│  ☑️  pgAdmin 4            ─── واجهة رسومية         │
│  ☑️  Stack Builder        ─── لتثبيت إضافات        │
│  ☑️  Command Line Tools   ─── أدوات الـ CLI        │
└─────────────────────────────────────────────────────┘

✅ خلّي كل الخيارات مختارة (default)
```

اضغط **Next**

</div>

---

## 💾 الخطوة 5: مسار البيانات

<div dir="rtl">

### Data Directory

```
Default Path:
C:\Program Files\PostgreSQL\16\data

┌─────────────────────────────────────────────────────┐
│  ⚠️ مهم جداً:                                       │
│                                                      │
│  هنا هتتخزن كل الـ Databases والبيانات              │
│  تأكد إن فيه مساحة كافية على الـ Disk               │
│                                                      │
│  💡 في Production:                                  │
│  حط البيانات على Disk منفصل (SSD)                  │
└─────────────────────────────────────────────────────┘
```

اضغط **Next**

</div>

---

## 🔐 الخطوة 6: كلمة السر

<div dir="rtl">

### Password for postgres superuser

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│  Password: ••••••••••                               │
│  Retype:   ••••••••••                               │
│                                                      │
│  ⚠️ مهم جداً: احفظ الـ Password دي!                 │
│  هتحتاجها كل ما تتصل بالـ Database                  │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### نصائح لاختيار Password

```
✅ كويس:
- MyP@ssw0rd123
- Postgres#2024!
- DevDB_Secure99

❌ وحش:
- 123456
- password
- postgres
```

> 💡 **نصيحة:** في Development ممكن تستخدم حاجة بسيطة زي `postgres123`، بس في Production لازم password قوية جداً.

اضغط **Next**

</div>

---

## 🔌 الخطوة 7: رقم الـ Port

<div dir="rtl">

### Port Number

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│  Port: 5432                                         │
│                                                      │
│  ✅ خلّيه 5432 (Default)                            │
│                                                      │
│  💡 معلومة:                                         │
│  5432 هو الـ Port الافتراضي لـ PostgreSQL          │
│  لو عندك PostgreSQL قديم، ممكن يطلب port تاني     │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### لو الـ Port مشغول

```
Error: Port 5432 is already in use

الحل:
1. شوف إيه اللي شغال على الـ Port:
   netstat -ano | findstr 5432

2. اختار port تاني زي:
   5433, 5434, etc.
```

اضغط **Next**

</div>

---

## 🌍 الخطوة 8: Locale

<div dir="rtl">

### Locale Selection

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│  Locale: [Default locale]                           │
│                                                      │
│  💡 للتطبيقات العربية:                              │
│  ممكن تختار: Arabic_Saudi Arabia                   │
│                                                      │
│  ✅ للأغلب: خلّيها Default                          │
│                                                      │
└─────────────────────────────────────────────────────┘
```

اضغط **Next**

</div>

---

## ✅ الخطوة 9: ملخص وبدء التثبيت

<div dir="rtl">

### Pre Installation Summary

```
┌─────────────────────────────────────────────────────┐
│  Installation Directory: C:\Program Files\...\16    │
│  Server Installation: Yes                           │
│  pgAdmin: Yes                                       │
│  Port: 5432                                         │
│  Locale: [Default]                                  │
└─────────────────────────────────────────────────────┘
```

1. **راجع الإعدادات**
2. **اضغط Next**
3. **استنى التثبيت يخلص (2-5 دقائق)**

```
Installing...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100%
```

</div>

---

## 🏁 الخطوة 10: إنهاء التثبيت

<div dir="rtl">

### Completing the Setup

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│  ☐ Launch Stack Builder at exit?                    │
│                                                      │
│  💡 شيل العلامة دي                                  │
│  Stack Builder لتثبيت extensions إضافية            │
│  مش محتاجينه دلوقتي                                │
│                                                      │
└─────────────────────────────────────────────────────┘
```

اضغط **Finish**

</div>

---

## ✔️ التحقق من التثبيت

<div dir="rtl">

### 1. من Command Prompt

</div>

```powershell
# افتح CMD أو PowerShell

# تحقق من الإصدار
psql --version
```

**النتيجة المتوقعة:**
```
psql (PostgreSQL) 16.1
```

<div dir="rtl">

### 2. تحقق إن الـ Service شغال

</div>

```powershell
# من PowerShell (كـ Administrator)
Get-Service -Name "postgresql*"
```

**النتيجة المتوقعة:**
```
Status   Name               DisplayName
------   ----               -----------
Running  postgresql-x64-16  postgresql-x64-16 - PostgreSQL Server
```

<div dir="rtl">

### 3. الاتصال بالـ Database

</div>

```powershell
# الاتصال كـ postgres user
psql -U postgres

# هيطلب منك الـ Password
Password for user postgres: ********
```

**النتيجة المتوقعة:**
```
psql (16.1)
Type "help" for help.

postgres=#
```

<div dir="rtl">

### 4. تنفيذ أمر تجريبي

</div>

```sql
-- داخل psql
SELECT version();
```

**النتيجة:**
```
                          version
------------------------------------------------------------
 PostgreSQL 16.1, compiled by Visual C++ build 1937, 64-bit
```

```sql
-- للخروج
\q
```

---

## 🔧 إضافة psql للـ PATH

<div dir="rtl">

### لو أمر psql مش شغال

لو ظهرلك:
```
'psql' is not recognized as an internal or external command
```

### الحل:

1. **افتح System Properties**
   - اضغط `Win + R`
   - اكتب `sysdm.cpl`
   - اضغط Enter

2. **روح لـ Environment Variables**
   - Tab: Advanced
   - اضغط "Environment Variables"

3. **عدّل الـ Path**
   - تحت "System variables"
   - اختار "Path"
   - اضغط "Edit"
   - اضغط "New"
   - أضف:
   ```
   C:\Program Files\PostgreSQL\16\bin
   ```

4. **اضغط OK في كل النوافذ**

5. **افتح CMD جديد وجرب تاني**

</div>

---

## 🔄 تشغيل PostgreSQL كـ Service

<div dir="rtl">

### التحكم في الـ Service

</div>

```powershell
# تشغيل
net start postgresql-x64-16

# إيقاف
net stop postgresql-x64-16

# إعادة تشغيل
net stop postgresql-x64-16 && net start postgresql-x64-16
```

<div dir="rtl">

### من Services Manager

1. اضغط `Win + R`
2. اكتب `services.msc`
3. دوّر على `postgresql-x64-16`
4. Right-click → Start/Stop/Restart

</div>

---

## ❌ حل المشاكل الشائعة

<div dir="rtl">

### مشكلة 1: Port 5432 مشغول

</div>

```powershell
# شوف مين شغال على الـ Port
netstat -ano | findstr 5432

# النتيجة هتكون زي:
TCP    0.0.0.0:5432    0.0.0.0:0    LISTENING    1234
                                                  ↑ PID

# شوف الـ Process
tasklist | findstr 1234
```

<div dir="rtl">

**الحل:**
- أقفل الـ Process اللي شغال
- أو استخدم Port تاني وقت التثبيت

### مشكلة 2: Password مش شغالة

</div>

```powershell
# جرب الاتصال بـ Trust authentication
psql -U postgres -h 127.0.0.1
```

<div dir="rtl">

**لو فشل:**
1. روح لـ `C:\Program Files\PostgreSQL\16\data`
2. افتح `pg_hba.conf`
3. غيّر `scram-sha-256` لـ `trust` مؤقتاً
4. أعد تشغيل الـ Service
5. اتصل وغيّر الـ Password

### مشكلة 3: Service مش بيشتغل

</div>

```powershell
# شوف الـ Log
Get-Content "C:\Program Files\PostgreSQL\16\data\log\*.log" -Tail 50
```

<div dir="rtl">

**الأسباب الشائعة:**
- Data directory permissions
- Port مشغول
- Disk full

### مشكلة 4: psql مش موجود

- تأكد إنك ثبّت "Command Line Tools"
- أضف المسار للـ PATH (شرحناه فوق)

</div>

---

## ✅ Checklist

<div dir="rtl">

تأكد من كل الخطوات دي:

- [ ] ✅ PostgreSQL installed
- [ ] ✅ `psql --version` شغال
- [ ] ✅ Service running
- [ ] ✅ يقدر يتصل بـ `psql -U postgres`
- [ ] ✅ Password محفوظة في مكان آمن

</div>

---

## 📝 ملخص

<div dir="rtl">

### ملفات ومسارات مهمة

```
Installation:     C:\Program Files\PostgreSQL\16\
Data Directory:   C:\Program Files\PostgreSQL\16\data\
Config File:      C:\Program Files\PostgreSQL\16\data\postgresql.conf
Auth File:        C:\Program Files\PostgreSQL\16\data\pg_hba.conf
Log Files:        C:\Program Files\PostgreSQL\16\data\log\
```

### أوامر مهمة

```powershell
# Version
psql --version

# Connect
psql -U postgres

# Service control
net start postgresql-x64-16
net stop postgresql-x64-16
```

</div>

---

## ⏭️ الدرس التالي

<div dir="rtl">

**➡️ [تثبيت PostgreSQL على Linux](./03-installation-linux.md)**

أو لو مش محتاج Linux:

**➡️ [إعداد pgAdmin](./05-pgadmin-setup.md)**

</div>

---

<div align="center">

[⬅️ السابق: ما هو PostgreSQL](./01-what-is-postgresql.md) | [🏠 العودة للـ Module](../README.md)

</div>
